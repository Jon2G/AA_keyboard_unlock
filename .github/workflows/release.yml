name: Release APK

on:
  workflow_dispatch:
    inputs:
      version_name:
        description: 'Release version (e.g. 1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Decode keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > aa-keyboard-unlock.keystore
          size=$(stat -c%s aa-keyboard-unlock.keystore 2>/dev/null || stat -f%z aa-keyboard-unlock.keystore)
          if [ "$size" -lt 100 ]; then
            echo "aa-keyboard-unlock.keystore is too small ($size bytes). Check ANDROID_KEYSTORE_BASE64 secret."
            exit 1
          fi
          echo "Keystore decoded ($size bytes)"

      - name: Validate signing secrets
        run: |
          if [ -z "${{ secrets.ANDROID_SIGNING_PASSWORD }}" ]; then
            echo "ANDROID_SIGNING_PASSWORD secret is not set."
            exit 1
          fi

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Resolve version
        id: version
        working-directory: .
        run: |
          VERSION_NAME="${{ github.event.inputs.version_name }}"

          IFS='.' read -r MAJOR MINOR PATCH <<< "${VERSION_NAME%%-*}"
          MAJOR=${MAJOR:-0}
          MINOR=${MINOR:-0}
          PATCH=${PATCH:-0}
          VERSION_CODE=$((MAJOR * 10000 + MINOR * 100 + PATCH))

          echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "version_code=$VERSION_CODE" >> "$GITHUB_OUTPUT"
          echo "apk_name=aa-keyboard-unlock-${VERSION_NAME}.apk" >> "$GITHUB_OUTPUT"
          echo "log_apk_name=aa-keyboard-unlock-${VERSION_NAME}-log.apk" >> "$GITHUB_OUTPUT"
          echo "tag_name=v${VERSION_NAME}" >> "$GITHUB_OUTPUT"

      - name: Create local.properties
        run: echo "sdk.dir=${ANDROID_SDK_ROOT}" >> local.properties

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build release APKs
        env:
          ANDROID_SIGNING_PASSWORD: ${{ secrets.ANDROID_SIGNING_PASSWORD }}
        run: |
          ./gradlew assembleRelease assembleLogging \
            -PversionName="${{ steps.version.outputs.version_name }}" \
            -PversionCode="${{ steps.version.outputs.version_code }}"

      - name: Rename APKs
        run: |
          pick_apk() {
            local dir="$1"
            if [ -f "app/build/outputs/apk/${dir}/app-${dir}.apk" ]; then
              echo "app/build/outputs/apk/${dir}/app-${dir}.apk"
            else
              echo "app/build/outputs/apk/${dir}/app-${dir}-unsigned.apk"
            fi
          }
          cp "$(pick_apk release)" "../${{ steps.version.outputs.apk_name }}"
          cp "$(pick_apk logging)" "../${{ steps.version.outputs.log_apk_name }}"

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: aa-keyboard-unlock-${{ steps.version.outputs.version_name }}
          path: |
            ${{ steps.version.outputs.apk_name }}
            ${{ steps.version.outputs.log_apk_name }}

      - name: Prepare release notes
        working-directory: .
        run: |
          VERSION="${{ steps.version.outputs.version_name }}"
          cat > release_body.md <<EOF
          ## AA Keyboard Unlock v${VERSION}

          LSPosed module that unlocks the Android Auto on-screen keyboard while driving.

          ### Install

          1. Install \`aa-keyboard-unlock-${VERSION}.apk\` on a rooted device with LSPosed
          2. LSPosed Manager → enable module → scope **Android Auto** and **Google Maps**
          3. Open **AA Keyboard Unlock** → enable **Unlock keyboard while active**
          4. Force-stop Android Auto and reconnect to your head unit

          **Logging build:** \`aa-keyboard-unlock-${VERSION}-log.apk\` enables verbose MAPS-DRIVE traces in LSPosed log (for development / DHU capture). Same behavior otherwise.

          ### Tested with

          - Android Auto \`17.1.662404-release\`
          - LSPosed API 82

          ### What it unlocks

          - External app text fields (WhatsApp reply, messaging, etc.)
          - Google Maps Car search bar
          - Car App SearchTemplate / Voice Plate keyboard hints

          Voice dictation is blocked; the QWERTY keyboard opens instead.

          ### Requirements

          - Rooted device (Magisk or KernelSU) with LSPosed
          - Android Auto + Google Maps installed

          See [README](https://github.com/${{ github.repository }}/blob/main/README.md) and [CHANGELOG](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for full details.

          ### Safety

          Disabling driving-distraction protections may be illegal in your jurisdiction. Use only when safe and legal. Module defaults to **disabled**.
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: v${{ steps.version.outputs.version_name }}
          files: |
            ${{ steps.version.outputs.apk_name }}
            ${{ steps.version.outputs.log_apk_name }}
          body_path: release_body.md
